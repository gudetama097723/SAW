<div class="container py-5">
  <div class="card bg-dark text-light border-secondary col-md-5 mx-auto">
    <div class="card-header">新規登録</div>
    <div class="card-body">
      <% if alert.present? %>
        <div class="alert alert-warning"><%= alert %></div>
      <% end %>

      <%= form_with model: @user, url: signup_path, class: "d-grid gap-3" do |form| %>
        <div>
          <%= form.label :username, "ユーザー名", class: "form-label" %>
          <%= form.text_field :username, class: "form-control", autocomplete: "username" %>
        </div>
        <div>
          <%= form.label :password, "パスワード", class: "form-label" %>
          <%= form.password_field :password, class: "form-control", autocomplete: "new-password" %>
        </div>
        <%= form.submit "登録して開始", class: "btn btn-primary" %>
        <%= link_to "ログインへ", login_path, class: "btn btn-outline-light" %>
      <% end %>
    </div>
  </div>
</div>
